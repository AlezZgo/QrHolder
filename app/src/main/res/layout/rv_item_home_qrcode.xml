<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="6dp"
    app:cardUseCompatPadding="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:ignore="UseCompoundDrawables">


        <ImageView
            android:id="@+id/qrCodeImageView"
            android:layout_width="@dimen/qrCodeStandardSize"
            android:layout_height="@dimen/qrCodeStandardSize"
            android:contentDescription="@string/qrcodepreview"
            app:srcCompat="@drawable/ic_app" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            tools:ignore="UseCompoundDrawables">

            <TextView
                android:id="@+id/titleTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:fontFamily="@font/roboto"
                android:gravity="start|center_vertical"
                android:maxLines="1"
                android:padding="8dp"
                android:textSize="20sp"
                android:textStyle="bold"
                tools:text="Ресторан Астория" />

            <TextView
                android:id="@+id/contentTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:fontFamily="@font/roboto"
                android:gravity="top|center_vertical"
                android:maxLines="1"
                android:padding="8dp"
                android:textSize="14sp"
                tools:text="https://www.astoria_restaurant.com" />
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>